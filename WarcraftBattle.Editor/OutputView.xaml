<UserControl x:Class="WarcraftBattle.Editor.OutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:WarcraftBattle.Editor.Properties">
    <Border Background="{StaticResource Editor.Panel.Background}" BorderBrush="{StaticResource Editor.Border.Base}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="{x:Static res:Strings.OutputTitle}" Margin="8,4" FontWeight="Bold" />
            <ListBox Grid.Row="1" ItemsSource="{Binding Messages}" />
        </Grid>
    </Border>
</UserControl>
