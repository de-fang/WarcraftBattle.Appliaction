<Application x:Class="WarcraftBattle.Editor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WarcraftBattle.Editor">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <Color x:Key="Brush.Window.Background">#1E1E1E</Color>
                    <Color x:Key="Brush.Panel.Background">#252526</Color>
                    <Color x:Key="Brush.Border.Base">#3F3F46</Color>
                    <Color x:Key="Brush.Control.Background">#333337</Color>
                    <Color x:Key="Brush.Control.Foreground">#F1F1F1</Color>
                    <Color x:Key="Brush.Accent">#007ACC</Color>
                    <Color x:Key="Brush.Accent.Orange">#CA5100</Color>
                    <Color x:Key="Brush.Grid.Line">#404040</Color>

                    <SolidColorBrush x:Key="Editor.Window.Background" Color="{StaticResource Brush.Window.Background}" />
                    <SolidColorBrush x:Key="Editor.Panel.Background" Color="{StaticResource Brush.Panel.Background}" />
                    <SolidColorBrush x:Key="Editor.Border.Base" Color="{StaticResource Brush.Border.Base}" />
                    <SolidColorBrush x:Key="Editor.Control.Background" Color="{StaticResource Brush.Control.Background}" />
                    <SolidColorBrush x:Key="Editor.Control.Foreground" Color="{StaticResource Brush.Control.Foreground}" />
                    <SolidColorBrush x:Key="Editor.Accent" Color="{StaticResource Brush.Accent}" />
                    <SolidColorBrush x:Key="Editor.Accent.Orange" Color="{StaticResource Brush.Accent.Orange}" />
                    <SolidColorBrush x:Key="Editor.Grid.Line" Color="{StaticResource Brush.Grid.Line}" />

                    <Style TargetType="Window">
                        <Setter Property="Background" Value="{StaticResource Editor.Window.Background}" />
                        <Setter Property="Foreground" Value="{StaticResource Editor.Control.Foreground}" />
                        <Setter Property="WindowStyle" Value="None" />
                        <Setter Property="AllowsTransparency" Value="False" />
                    </Style>

                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="{StaticResource Editor.Control.Background}" />
                        <Setter Property="Foreground" Value="{StaticResource Editor.Control.Foreground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Editor.Border.Base}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="6,4" />
                    </Style>

                    <Style TargetType="ComboBox">
                        <Setter Property="Background" Value="{StaticResource Editor.Control.Background}" />
                        <Setter Property="Foreground" Value="{StaticResource Editor.Control.Foreground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Editor.Border.Base}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="4,2" />
                    </Style>

                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource Editor.Control.Background}" />
                        <Setter Property="Foreground" Value="{StaticResource Editor.Control.Foreground}" />
                        <Setter Property="BorderBrush" Value="{StaticResource Editor.Border.Base}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="6,4" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="{x:Type GridSplitter}">
                        <Setter Property="Width" Value="5" />
                        <Setter Property="Background" Value="{StaticResource Editor.Panel.Background}" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>

                    <Style TargetType="{x:Type ScrollBar}">
                        <Setter Property="Width" Value="10" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid Background="{TemplateBinding Background}">
                                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" />
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" />
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb>
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                                            <Border x:Name="ThumbBorder" Background="#88444444" CornerRadius="2" />
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="true">
                                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#AA666666" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="{x:Type TabControl}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabControl}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TabPanel Grid.Row="0"
                                                  IsItemsHost="True"
                                                  Background="{StaticResource Editor.Panel.Background}" />
                                        <Border Grid.Row="1"
                                                BorderBrush="{StaticResource Editor.Border.Base}"
                                                BorderThickness="1"
                                                Background="{StaticResource Editor.Window.Background}">
                                            <ContentPresenter ContentSource="SelectedContent" />
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="Foreground" Value="{StaticResource Editor.Control.Foreground}" />
                        <Setter Property="Background" Value="{StaticResource Editor.Panel.Background}" />
                        <Setter Property="Padding" Value="8,4" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{StaticResource Editor.Border.Base}"
                                            BorderThickness="1,1,1,0"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentSource="Header" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource Editor.Control.Background}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
                <ResourceDictionary>
                    <local:AppBootstrapper x:Key="Start" />
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
